package types

// GitTag represents a git tag reference (e.g., "v1.0.0")
type GitTag string

// GitCommitSHA represents a git commit SHA
type GitCommitSHA string

// GitRefName represents a git reference name (branch, tag, etc.)
type GitRefName string
